# Railway — backup deployment (does not affect Vercel).
# Docs: docs/BACKUP_DEPLOYMENT.md

[build]
builder = "NIXPACKS"
# Install phase already runs `npm ci` — do not run it again (EBUSY on cache mounts).
buildCommand = "node scripts/write-build-info.mjs && npx prisma generate && npm run build"

[deploy]
startCommand = "npx prisma migrate deploy && npm run start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
