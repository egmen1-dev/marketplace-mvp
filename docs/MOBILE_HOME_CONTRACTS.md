# Mobile Home Contracts

## Seller

`GET /api/mobile/seller/home` — compact seller mode dashboard foundation.

## Buyer

`GET /api/mobile/buyer/home` — discovery / favourites / orders foundation.

Both advisory-only payloads for future App Shell 0.

See `docs/MOBILE_HOME_CONTRACTS.md` implementation in `lib/mobile/seller-home.ts` and `buyer-home.ts`.
